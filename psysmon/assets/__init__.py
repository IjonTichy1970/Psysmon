"""Bundled static assets shipped with psysmon (e.g. the status-page logo).

Loaded at runtime via :func:`importlib.resources.files` so the daemon can deploy the logo next
to the status file it writes; see :mod:`psysmon.output.statuspage`.
"""
