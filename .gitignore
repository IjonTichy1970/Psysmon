# Legacy C source / review scratch (read-only reference, not part of the rewrite)
_review/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.installed.cfg
*.egg

# Generated user-guide HTML (#64); built by tools/build_guide.py + CI for GitHub Pages.
# The consolidated docs/guide/psysmon-guide.txt IS committed (ships in the sdist).
site/

# Virtual environments
.venv/
venv/
env/

# Test / coverage / type-checker caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# Editors / OS / local tooling
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
# Per-developer Claude Code config + workflow skills (not shared in the repo)
.claude/

# Local runtime artifacts
*.log
psysmon.stat.html*
status.html*

# Real production config kept local for scale/smoke checks (contains live data + creds)
tests/fixtures/production.conf
